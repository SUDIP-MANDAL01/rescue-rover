databases:
  - name: rescue-rover-db
    databaseName: rescuerover
    plan: free

services:
  - type: web
    name: rescue-rover-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rescue-rover-db
          property: connectionString
